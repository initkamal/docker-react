sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t kamalyadav/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kamalyadav/docker-react npm run test -- --coverage
